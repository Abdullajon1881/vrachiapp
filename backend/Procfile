web: python manage.py collectstatic --noinput && python manage.py migrate && python manage.py shell -c "from authentication.models import User; User.objects.filter(email='admin@healzy.com').delete(); u=User.objects.create_superuser(email='admin@healzy.com', password='Admin1234!', username='admin'); u.is_active=True; u.save(); print('done')" && gunicorn vrachiapp_backend.asgi:application -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
